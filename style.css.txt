body {
    font-family: 'Segoe UI', sans-serif;
    margin: 0;
    background: #fff8e6 url('https://i.imgur.com/1UUlYtR.png');
    background-size: cover;
}

header {
    text-align: center;
    padding: 35px;
    background: linear-gradient(90deg, #ff0000, #ff7b00);
    color: white;
    border-bottom: 6px solid #ffd54f;
}

h1 {
    font-size: 34px;
    margin-bottom: 5px;
    font-weight: 900;
    text-shadow: 2px 2px #b30000;
}

.desc {
    font-size: 17px;
    opacity: 0.95;
    font-weight: 500;
}

.decor {
    text-align: center;
    margin-top: 10px;
}

.budget-input {
    text-align: center;
    margin: 25px 0;
}

input {
    padding: 10px;
    width: 250px;
    border-radius: 6px;
    border: 2px solid #ff9800;
    background: #fff9ec;
    font-size: 16px;
}

button {
    padding: 10px 18px;
    border: none;
    background: #ff0000;
    color: white;
    border-radius: 8px;
    cursor: pointer;
    margin-left: 6px;
    font-size: 16px;
    font-weight: bold;
    box-shadow: 0 4px 6px rgba(0,0,0,0.25);
}

button:hover {
    background: #cc0000;
}

.cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(290px, 1fr));
    gap: 20px;
    padding: 20px;
}

/* Thẻ mục */
.card {
    background: white url('https://i.imgur.com/oM8Cq1Z.png');
    background-size: cover;
    padding: 20px;
    border-radius: 12px;
    border-left: 10px solid #ff3d00;
    box-shadow: 0 4px 10px rgba(0,0,0,0.15);
}

.card h3 {
    margin-top: 0;
    color: #b71c1c;
    font-weight: 800;
}

.card ul {
    padding-left: 18px;
    background: rgba(255,255,255,0.85);
    border-radius: 8px;
    padding: 10px 12px;
}

.card ul li {
    margin: 8px 0;
    font-size: 15px;
    font-weight: 500;
}

.money {
    color: #ff0000;
    font-weight: 900;
    text-shadow: 1px 1px #ffcc00;
}
